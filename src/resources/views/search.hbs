<div class="mt-4">
    <form method="POST"> 
    {{!-- POST: gửi DL của form dưới dạng Form data (không hiển thị trên URL như query parameter trong p.thức GET) --}}
        <div class="form-group">
            <label for="search-input">Tìm kiếm</label>
            <input type="text" name="q" class="form-control" id="search-input" placeholder="Nhập từ khóa">
        </div>

        <div class="form-group">
            <label for="gender-input">Tìm kiếm</label>
            <select name="gender" class="form-control" id="gender-input">
                <option value="">Chọn giới tính</option>
                <option value="male">Nam</option>
                <option value="female">Nữ</option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Tìm kiếm</button>
    </form>
</div>