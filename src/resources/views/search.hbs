<div class="mt-4">
    <form method="GET" action="/news"> 
    {{!-- action: thay đổi requestURL -> submit ở trang nào -> khi bấm submit, thông tin và phương thức của form sẽ redirect sang đường dẫn trong action --}}
        <div class="form-group">
            <label for="search-input">Tìm kiếm</label>
            <input type="text" name="q" class="form-control" id="search-input" placeholder="Nhập từ khóa">
        </div>

        <button type="submit" class="btn btn-primary">Tìm kiếm</button>
    </form>
</div>